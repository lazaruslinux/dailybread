# Web framework and the ASGI server that runs it
fastapi
uvicorn[standard]

# Reads settings from environment variables / .env
pydantic-settings

# Database: SQLAlchemy (Core/ORM) + the psycopg3 driver (postgresql+psycopg://)
sqlalchemy
psycopg[binary]

# Schema migrations, run automatically at container startup
alembic

# Auth: Argon2 password hashing + JWT (PyJWT) session tokens
argon2-cffi
pyjwt

# Avatar uploads: multipart form parsing + image decode/crop/resize/encode
python-multipart
pillow

# Outbound food-database lookups (USDA FoodData Central, Open Food Facts)
httpx

# Web Push reminders (VAPID signing + payload encryption)
pywebpush
