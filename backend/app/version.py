"""The running version, kept in step with frontend/package.json by the release
round (a test pins the two together). A boot that finds a different version
stored in app_meta announces itself to every parent."""

APP_VERSION = "2.1.0"
