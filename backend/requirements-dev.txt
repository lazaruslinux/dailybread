# Test-only dependencies; production images never install these.
pytest
httpx
# Pinned: ruff's default rule set changes between releases, so an unpinned
# linter fails CI on its own schedule rather than on ours.
ruff==0.16.0
